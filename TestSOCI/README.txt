1. 分别针对 1000 条数据和 50000 条数据测试
2. 针对以下特性
prepare statement(st.execute(true);) vs NOT prepare
transaction 分多次提交事务 vs 只提交一次事务
bulk operations
先建立索引 vs 后建立索引
replace into vs insert
多线程写入


Note
vcpkg 的 soci 库无法使用？